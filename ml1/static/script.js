document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('recommendation-form');
    form.addEventListener('submit', (event) => {
        // Additional JavaScript logic can be added here if necessary.
    });
});
